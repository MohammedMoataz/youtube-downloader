# Base URL of the separate, self-hosted yt-dlp extraction service the site calls at runtime.
# This is a PUBLIC value (no secret). For local dev, point it at the mock: `npm run mock-api`.
PUBLIC_DOWNLOADER_API=http://localhost:8787
